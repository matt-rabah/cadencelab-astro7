---
// src/components/Hero.astro
---

<section class="hero-section" aria-labelledby="hero-title">
  <div class="hero-container">
    <div class="hero-content">
      <p class="hero-eyebrow">Customer experience systems</p>

      <h1 id="hero-title" class="hero-title">
        AI isn't your customer experience problem.
        <span class="hero-accent">Adoption is.</span>
      </h1>

      <p class="hero-subtitle">
        Most customer-facing AI initiatives fail because of implementation,
        trust, and measurement—not the model. We translate stakeholder needs,
        operational constraints, and CRM data into workflows people can
        understand, adopt, and improve.
      </p>

      <div class="hero-actions">
        <a href="/fit-check/" class="hero-button hero-button-primary">
          Start a fit check
        </a>

        <a href="#framework" class="hero-button hero-button-secondary">
          Explore the adoption framework
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    width: 100%;
    padding-block: clamp(5rem, 11vw, 9rem);
    background: var(--color-surface);
    color: var(--color-text);
  }

  .hero-container {
    width: min(calc(100% - (2 * var(--page-gutter))), var(--container-page));
    margin-inline: auto;
  }

  .hero-content {
    display: flex;
    max-width: 62rem;
    flex-direction: column;
    align-items: flex-start;
    gap: clamp(1.5rem, 3vw, 2rem);
  }

  .hero-eyebrow {
    margin: 0;
    color: var(--color-blue);
    font-family: var(--font-sans);
    font-size: 0.8125rem;
    font-style: normal;
    font-weight: 600;
    line-height: 1.4;
    letter-spacing: 0;
    text-transform: none;
  }

  .hero-title {
    max-width: 15ch;
    margin: 0;
    color: var(--color-ink);
    font-family: var(--font-sans);
    font-size: clamp(3rem, 1.9rem + 4.5vw, 6.5rem);
    font-style: normal;
    font-weight: 600;
    line-height: 0.97;
    letter-spacing: -0.04em;
    text-wrap: balance;
  }

  .hero-accent {
    color: var(--color-blue);
  }

  .hero-subtitle {
    max-width: 46rem;
    margin: 0;
    color: var(--color-text-muted);
    font-family: var(--font-sans);
    font-size: clamp(1.0625rem, 1rem + 0.3vw, 1.25rem);
    font-style: normal;
    font-weight: 400;
    line-height: 1.6;
    letter-spacing: -0.01em;
    text-wrap: pretty;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.875rem;
    margin-top: 0.5rem;
  }

  .hero-button {
    display: inline-flex;
    width: fit-content;
    min-height: 3rem;
    align-items: center;
    justify-content: center;
    padding: 0.875rem 1.25rem;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
    font-family: var(--font-sans);
    font-size: 0.9375rem;
    font-style: normal;
    font-weight: 600;
    line-height: 1.2;
    text-align: center;
    text-decoration: none;
    transition:
      background-color var(--transition-fast),
      border-color var(--transition-fast),
      color var(--transition-fast),
      transform var(--transition-fast);
  }

  .hero-button:hover {
    text-decoration: none;
  }

  .hero-button:active {
    transform: translateY(1px);
  }

  .hero-button-primary {
    border-color: var(--color-ink);
    background: var(--color-ink);
    color: var(--color-light);
  }

  .hero-button-primary:hover {
    border-color: color-mix(in srgb, var(--color-ink) 86%, var(--color-light));

    background: color-mix(in srgb, var(--color-ink) 86%, var(--color-light));

    color: var(--color-light);
  }

  .hero-button-secondary {
    border-color: var(--color-border-strong);
    background: transparent;
    color: var(--color-ink);
  }

  .hero-button-secondary:hover {
    border-color: var(--color-ink);
    background: var(--color-surface-pure);
    color: var(--color-ink);
  }

  .hero-button:focus-visible {
    outline: var(--focus-ring-width) solid var(--color-focus);
    outline-offset: var(--focus-ring-offset);
  }

  @media (max-width: 40rem) {
    .hero-section {
      padding-block: clamp(4rem, 18vw, 6rem);
    }

    .hero-title {
      max-width: 12ch;
      font-size: clamp(2.75rem, 12vw, 4.25rem);
      line-height: 0.99;
      letter-spacing: -0.035em;
    }

    .hero-subtitle {
      max-width: 36rem;
      font-size: 1.0625rem;
      line-height: 1.6;
    }

    .hero-actions {
      width: 100%;
      flex-direction: column;
      align-items: stretch;
      gap: 0.75rem;
    }

    .hero-button {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-button {
      transition: none;
    }
  }

  @media (forced-colors: active) {
    .hero-button {
      border-color: ButtonText;
    }
  }
</style>
